server:
  port: 8001

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false #不自己注册自己
    fetchRegistry: false  #不用去检查服务
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/