server:
  port: 8001

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false #因为自己是注册中心，false代表不向自己注册自己
    fetchRegistry: false  #自己是服务注册中心，职责是维护服务实例，false代表不用去检查服务
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/